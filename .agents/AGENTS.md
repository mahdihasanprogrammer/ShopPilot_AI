# ShopPilot AI — Project Rules

## Workflow Rules

- After completing each user prompt (a full feature, fix, or refactor), make a git commit with a descriptive message summarizing what was done. Use conventional commit format where applicable (e.g., `feat:`, `fix:`, `refactor:`, `style:`).
